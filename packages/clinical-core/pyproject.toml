[project]
name = "clinical-core"
version = "0.1.0"
description = "Shared clinical contracts for the Clinical Intelligence Platform"
requires-python = ">=3.11,<3.14"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/clinical_core"]
